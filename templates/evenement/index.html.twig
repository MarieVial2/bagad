{% extends 'baseAdmin.html.twig' %}

{% block title %}Evenement index{% endblock %}

{% block body %}
<div class="container">
    <h4>Agenda</h4>
    <p>Retrouvez ici tous les évènements du bagad, qui apparaitront sur <a href="{{ path('app_agenda')}}">la page
            agenda</a>. Pensez à supprimer les évènements passés ou ils apparaitront sur l'agenda ! </p>

    <div class="nouveau">
        <a href="{{ path('app_evenement_new') }}">
            <button type="button" class="btn btn-primary">Nouvel évènement</button>
        </a>
    </div>

    <table class="table table-hover">
        <thead>
            <tr>

                <th>Nom</th>
                <th>Date</th>
                <th>Lieu</th>

                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for evenement in evenements %}
            <tr>

                <td>{{ evenement.nomEvenement }}</td>
                <td>{{ evenement.dateEvenement ? evenement.dateEvenement|date('d-m-Y') : '' }}</td>
                <td>{{ evenement.lieuEvenement }}</td>
                <td class="groupe-boutons">

                    <a href="{{ path('app_evenement_show', {'id': evenement.id}) }}">
                        <button type="button" class="btn btn-warning">Voir</button>
                    </a>
                    <a href="{{ path('app_evenement_edit', {'id': evenement.id}) }}">
                        <button type="button" class="btn btn-success">Editer</button>
                    </a>
                    {{ include('evenement/_delete_form.html.twig') }}

                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="9">Aucun évènement trouvé</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>


</div>
{% endblock %}