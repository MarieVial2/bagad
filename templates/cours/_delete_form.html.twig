<form class="delete-form" method="post" action="{{ path('app_cours_delete', {'id': cour.id}) }}"
    onsubmit="return confirm('Etes-vous sûr.e de vouloir supprimer cela ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cour.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>