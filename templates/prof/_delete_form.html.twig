<form class="delete-form" method="post" action="{{ path('app_prof_delete', {'id': prof.id}) }}" onsubmit="return confirm('Etes-vous sûr.e de vouloir supprimer cela ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prof.id) }}">
    <button class="btn btn btn-danger">Supprimer</button>
</form>
