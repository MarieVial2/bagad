<form method="post" action="{{ path('app_demande_prestation_delete', {'id': demande_prestation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_prestation.id) }}">
    <button class="btn">Delete</button>
</form>
