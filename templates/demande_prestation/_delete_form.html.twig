<form class="delete-form" method="post"
    action="{{ path('app_demande_prestation_delete', {'id': demande_prestation.id}) }}"
    onsubmit="return confirm('Etes-vous sûr.e de vouloir supprimer cela ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_prestation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>